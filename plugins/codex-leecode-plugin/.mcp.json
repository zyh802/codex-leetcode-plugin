{
  "mcpServers": {
    "leetcode": {
      "type": "stdio",
      "command": "node",
      "args": [
        "-e",
        "const p=require('node:path'),u=require('node:url');const r=process.env.PLUGIN_ROOT||process.env.CLAUDE_PLUGIN_ROOT||process.cwd();import(u.pathToFileURL(p.join(r,'bootstrap.mjs')).href);"
      ]
    }
  }
}
